export default function Shop() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Shop Settings</h1>
      <p className="text-gray-500">ตั้งค่าข้อมูลร้าน (ชื่อ, รูป, สี, WhatsApp template) — เปิดจาก Worker /admin/shop</p>
    </div>
  );
}
